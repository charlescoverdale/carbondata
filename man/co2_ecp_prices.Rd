% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregators.R
\name{co2_ecp_prices}
\alias{co2_ecp_prices}
\title{Emissions-weighted carbon prices (ECP)}
\usage{
co2_ecp_prices(
  level = c("jurisdiction", "sector", "coverage", "coverage_sector", "region"),
  gas = "CO2",
  country = NULL,
  refresh = FALSE
)
}
\arguments{
\item{level}{Character. One of \code{"jurisdiction"} (default),
\code{"sector"}, \code{"coverage"}, \code{"coverage_sector"}, or \code{"region"}.}

\item{gas}{Character. Greenhouse gas. Default \code{"CO2"}.}

\item{country}{Optional character vector of jurisdiction names to
filter (matched case-insensitively, e.g. \code{"United Kingdom"}).}

\item{refresh}{Re-download? Default \code{FALSE}. Cached copies refresh
automatically after 30 days.}
}
\value{
A data frame.
}
\description{
Fetches the emissions-weighted carbon price dataset published
alongside the World Carbon Pricing Database at
\url{https://worldcarbonpricing.org}. Unlike the raw WCPD files, these
endpoints remain open and are updated with each release, so this is
the current route to World Carbon Pricing Database derived data.
}
\details{
\code{level = "jurisdiction"} returns average effective carbon prices by
jurisdiction and year and \code{"sector"} disaggregates by IPCC sector.
Coverage shares, the fraction of emissions subject to a price, come
from \code{"coverage"} (by jurisdiction) and \code{"coverage_sector"}.

\code{"region"} is accepted because the publisher documents the endpoint,
but their server currently has no data file behind it for any gas;
the call errors with that explanation until they restore it.
}
\examples{
\donttest{
op <- options(carbondata.cache_dir = tempdir())
ecp <- co2_ecp_prices(country = "United Kingdom")
options(op)
}
}
\references{
Dolphin, G., Xiahou, Q. (2022). "World carbon pricing database:
sources and methods." \emph{Scientific Data}, 9, 573.
\url{doi:10.1038/s41597-022-01659-x}
}
\seealso{
Other aggregators: 
\code{\link{co2_icap_prices}()},
\code{\link{co2_icap_systems}()},
\code{\link{co2_rff_pricing}()},
\code{\link{co2_world_bank}()}
}
\concept{aggregators}
